import React from "react";
import styled from "styled-components";

const Footer = styled.footer`
  background-color: #aaa;
  color: white;
  padding: 20px 0px;
`;

export default () => <Footer>© Movie Palette</Footer>;
